{
    "comment": "Copy this file to `config.json` and edit for your local setup. `incoming_data` is the path to the directory of input data, as provided - e.g. `~/data/vietnam-transport/C Incoming Data/`. `working_data` is the path to the directory of working data, input/output by these scripts",
    "paths": {
        "incoming_data": "./incoming_data",
        "working_data": "./data"
    }
}
